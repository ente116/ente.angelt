<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ente Angelt | Anthony</title>
    <style>
        body {
            overflow-x: hidden;
        }

        /* Grund-Setup */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body, html {
            height: 100%;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: #0f0f0f;
            color: #ffffff;
            overflow: hidden; /* Verhindert normales Scrollen */
        }

        /* Scroll Snap Container */
        .container {
            height: 100vh;
            overflow-y: scroll;
            scroll-snap-type: y mandatory; /* Magisches Einrasten */
        }

        section {
            height: 100vh;
            width: 100%;
            scroll-snap-align: start;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            padding: 20px;
            text-align: center;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('start.jpg') no-repeat center center/cover;
        }

        .hero2 {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('nays.jpg') no-repeat center center/cover;
        }

        .hero3 {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('3.jpg') no-repeat center center/cover;
        }

        .hero4 {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('2.jpg') no-repeat center center/cover;
        }

        .korean-bg {
            position: absolute;
            font-size: 15vw;
            color: rgba(117, 114, 114, 0.699);
            z-index: 1;
            font-weight: bold;
            white-space: nowrap;
        }

        .hero h1 {
            font-size: clamp(2rem, 5vw, 3.5rem); /* responsive */
            z-index: 2;
            letter-spacing: 2px;
            text-transform: none; /* ← wichtig */

        }

        .hero p {
            font-size: 1.2rem;
            color: #a0ff00; /* Nays-Grün Akzent */
            z-index: 2;
        }

        /* Tackle Section */
        .tackle-card {
            background: #1a1a1a;
            border: 1px solid #333;
            border-radius: 15px;
            padding: 30px;
            max-width: 400px;
            transition: transform 0.3s ease;
        }

        .tackle-card:hover {
            transform: scale(1.05);
            border-color: #a0ff00;
        }

        h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #fff;
        }

        ul {
            list-style: none;
            line-height: 2;
            font-size: 1.1rem;
        }

        .highlight {
            color: #a0ff00;
            font-weight: bold;
        }

        /* Galerie-Platzhalter */
        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            max-width: 500px;
        }

        .gallery-item {
            width: 100%;
            aspect-ratio: 1;
            background: #222;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
            color: #555;
        }

        /* Swipe Indicator */
        .arrow {
            position: absolute;
            bottom: 30px;
            animation: bounce 2s infinite;
            font-size: 1.5rem;
            color: #a0ff00;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {transform: translateY(0);}
            40% {transform: translateY(-10px);}
            60% {transform: translateY(-5px);}
        }

        @media (max-width: 768px) {

        .hero h1 {
            font-size: 2rem;
        }

        .hero p {
            font-size: 1rem;
        }

        .gallery-grid {
            grid-template-columns: 1fr; /* nur 1 Bild pro Reihe */
        }

        .tackle-card {
            padding: 20px;
            max-width: 90%;
        }

        h2 {
            font-size: 1.8rem;
        }

        img {
            max-width: 100%;
            height: auto;
        }

    }
    </style>
</head>
<body>

<div class="container">
    
    <section class="hero">
        <div class="korean-bg">앤토니</div>
        <h1>ente.angelt</h1>
        <p>Modern Predator Fishing</p>
        <div class="arrow">↓</div>
    </section>

    <section class="hero2">
        <h2>Der Vibe</h2>
        <p style="max-width: 600px;">Kein alter Hut, keine Langeweile. Antony bringt den modernen Streetfishing-Style ans Wasser. High-End Baits und die Liebe zum Detail.</p>
    </section>

    <section class="hero3">
        <div class="tackle-card">
            <h2>The Gear</h2>
            <ul>
                <li>Rods: <span class="highlight">Nays One</span></li>
                <li>Reels: <span class="highlight">Shimano Curado</span></li>
                <li>Baits: <span class="highlight">Nays MD MX / VNM</span></li>
            </ul>
        </div>
    </section>

    <section class="hero4">
        <h2>Recent Catches</h2>
        <div class="gallery-grid">
            <div class="gallery-item">PHOTO</div>
            <div class="gallery-item">PHOTO</div>
            <div class="gallery-item">PHOTO</div>
            <div class="gallery-item">PHOTO</div>
        </div>
        <p style="margin-top: 30px;"><a href="https://www.instagram.com//ente.angelt" style="color: #a0ff00; text-decoration: none;">Follow on Instagram →</a></p>
    </section>

</div>

</body>
</html>